count by (deviceslot, component, version) (
  baremetal_node_physical_topology_labels{
    label_ds_coreweave_com_physical_topology_rack_name="{rack_name}"
  }
  * on (region, deviceslot) group_left(component, version)
  redfish_firmware_version
)
